import { AppRouter } from './router/AppRouter'

/**
 * Componente raiz da aplicação.
 * Renderiza o roteador que cuida de toda a navegação.
 */
export default function App() {
  return <AppRouter />
}
